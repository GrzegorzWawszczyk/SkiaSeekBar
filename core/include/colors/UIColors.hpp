#pragma once

#include <core/SkColor.h>

namespace UIColors
{

    const SkColor Background = SkColorSetRGB(25, 25, 25);
    const SkColor Off = SkColorSetRGB(180, 180, 180);
    const SkColor On = SkColorSetRGB(100, 100, 100);
    const SkColor Accent = SkColorSetRGB(240, 240, 240);
    const SkColor Active = SkColorSetRGB(60, 60, 60);
    const SkColor Text = Off;
}
